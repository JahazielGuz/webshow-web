// How the player leaves: "back" when it was opened over the page the viewer was on (a
// client-side navigation into the modal slot), "home" when it was loaded directly
export type PlayerExit = "back" | "home";
