// How an overlay leaves: "back" when it was opened over the page the viewer was on (a
// client-side navigation into the modal slot), "home" when that URL was loaded directly
export type OverlayExit = "back" | "home";
